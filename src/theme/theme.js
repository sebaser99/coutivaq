import { createTheme, ThemeProvider } from '@mui/material';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#e1f8d9'
        },
        secondary: {
            main: '#26290289'
        }
    }
  })